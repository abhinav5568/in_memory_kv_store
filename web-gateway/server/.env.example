PORT=4000
CLIENT_ORIGIN=http://localhost:5173

# TCP engine (the same server your KVClient connects to)
TCP_ENGINE_HOST=127.0.0.1
TCP_ENGINE_PORT=6379

# JWT
JWT_SECRET=secretforkvstore
JWT_EXPIRES_IN=7d

# SQLite file location
SQLITE_DB_PATH=./data/gateway.db
